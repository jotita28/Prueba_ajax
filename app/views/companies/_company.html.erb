<tr id="<%=company.id%>">
<td id="<%=company.id%>"> </td>
<td><%= company.name %> </td> 
<td><%= link_to 'Show', company_path(company), class: 'btn btn-success btn-sm', data: {remote:true}%>
<%= link_to 'Edit', edit_company_path(company), class: 'btn btn-info btn-sm', data: {remote:true}%>
<%= link_to 'Destroy', company, method: :delete, class: 'btn btn-danger btn-sm', data: {confirm: 'Estas seguro?', remote: true}%></td>
</tr>